import { MaskToNgDatapicketFieldDirective } from './mask-to-ng-datapicket-field.directive';

describe('MaskToNgDatapicketFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskToNgDatapicketFieldDirective();
    expect(directive).toBeTruthy();
  });
});
